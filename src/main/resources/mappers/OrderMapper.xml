<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- namespace는 패키지 명 + 인터페이스명과 맞춰줘야한다. -->
<mapper namespace="edu.kosmo.krm.mapper.OrderMapper">

	<resultMap id="OrderMap" type="edu.kosmo.krm.joinVO.JoinOrderHistoryVO">
	
		<result property="status" column="status" />
		<result property="brand" column="brand" />
		<result property="product_name" column="product_name" />
		<result property="amount" column="amount" />
		<result property="order_id" column="order_id" />	
		<result property="order_date" column="order_date"/>
		<result property="img_id" column="img_id" />
	</resultMap>
	
	

	<select id="getOrderHistoryList" resultType="edu.kosmo.krm.joinVO.JoinOrderHistoryVO">
	   <![CDATA[
              SELECT * FROM (
              SELECT ROWNUM AS RNUM, A.* FROM (
                       SELECT
                             MEMBER_ORDER.STATUS,
                             PRODUCT.BRAND,
                             PRODUCT.NAME as product_name,
                             MEMBER_ORDER.ID as order_id,
                             MEMBER_ORDER.AMOUNT,
                             MEMBER_ORDER.ORDER_DATE,
                             PRODUCT_IMAGE.ID as img_id
                         FROM
                                    MEMBER,
                                    MEMBER_ORDER,
                                    DELIVERY,
                                    ORDER_DETAIL,
                                    PRODUCT,
                                    PRODUCT_IMAGE
                        WHERE
                                             MEMBER.ID = MEMBER_ORDER.MEMBER_ID
                        AND
                                             MEMBER_ORDER.ID = DELIVERY.ORDER_ID
                        AND
                                             MEMBER_ORDER.ID = ORDER_DETAIL.ORDER_ID
                        AND
                                             ORDER_DETAIL.PRODUCT_ID = PRODUCT.ID
                        AND
                                             PRODUCT.ID = PRODUCT_IMAGE.PRODUCT_ID
                        AND
                                             MEMBER.ID = #{memberVO.id}
              ) A WHERE ROWNUM <= #{criteria.pageNum} * #{criteria.amount}
         	) WHERE RNUM > (#{criteria.pageNum}-1) * #{criteria.amount}
	   ]]>
	</select>

 	<select id="getOrderHistoryTotalCount" resultType="int">
	   <![CDATA[
						SELECT count(*)
						FROM
						        MEMBER,
						        MEMBER_ORDER,
						        DELIVERY,
						        ORDER_DETAIL,
						        PRODUCT,
						        PRODUCT_IMAGE
						WHERE
						        MEMBER.ID = MEMBER_ORDER.MEMBER_ID
						AND
						        MEMBER_ORDER.ID = DELIVERY.ORDER_ID
						AND
						        MEMBER_ORDER.ID = ORDER_DETAIL.ORDER_ID
						AND
						        ORDER_DETAIL.PRODUCT_ID = PRODUCT.ID
						AND
						        PRODUCT.ID = PRODUCT_IMAGE.PRODUCT_ID				
						AND
						        MEMBER.ID = #{id}
	   ]]>
	</select>


	
</mapper> 